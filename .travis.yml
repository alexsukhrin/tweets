language: python

python:
  - "3.7"

services:
  - postgresql

#addons:
#  postgresql: "10"
#  apt:
#    packages:
#    - postgresql-10
#    - postgresql-client-10
#env:
#  global:
#  - POSTGRES_PORT=5433

install:
  - pip install codecov
  - pip install -r requirements.txt

script:
  - pytest --cov=./

after_script:
  - codecov
